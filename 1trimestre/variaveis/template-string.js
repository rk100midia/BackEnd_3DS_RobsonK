// Template (modelo) string ou template literals
// são textos que processam informações
// de natureza distinta muma mesma sentença 
// ex. "minha mãe é ${nomedamãe}"
// observe a estrutura padronizada com '${}'

const nome = 'Pedro'
const sobrenome = 'Marley'
const perfil = `Olá, ${nome} ${sobrenome}`
console.log(perfil)